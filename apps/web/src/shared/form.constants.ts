/** TaylorDB table for this form — also used as the forms-ui session / embed id. */
export const FORM_TABLE = "submissions" as const;

export const FORM_ID = FORM_TABLE;

export const FORM_COMPLETED_COLUMN = "submitted" as const;
